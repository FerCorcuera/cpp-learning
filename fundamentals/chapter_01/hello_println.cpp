#include <print>

int main()
{
  std::print("Hello, world!"); //in C++ every statement needs to end with semicolons
}
// lets think about std as a folder that has a set of functions
// It is ipmortant because we can name a function as any other,
// But only if they live in different "folders" (namespace)
// We can tink about the :: symbol as "take a look inside"

//indentation here is only for hvaing a clean code, but the compiler
//cares about the semilocons!
